<div class="agenda-cta">
		<div>
			<p class="invite-kicker">Planifica tu día.</p>
			<p class="invite-text">Consulta los horarios y salones de las charlas.</p>
		</div>
		<a href="/agenda">
			Ver agenda completa
			<svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true">
				<path d="M5 12h14m-6-6 6 6-6 6" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round" />
			</svg>
		</a>
</div>

<style>
	.agenda-cta {
		margin-block-start: 2rem;
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 1.5rem;
		padding: 1.75rem;
		border: 1px solid rgba(255, 255, 255, 0.12);
		border-radius: 12px;
		background: rgba(255, 255, 255, 0.04);
	}

	.invite-kicker {
		font-family: var(--font-display);
		font-weight: 700;
		font-size: 1.05rem;
		color: var(--color-text-inverse);
	}

	.invite-text {
		max-width: 48ch;
		margin-top: 0.4rem;
		color: var(--color-text-inverse-muted);
		font-size: 0.95rem;
	}

	a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 0.5rem;
		flex-shrink: 0;
		max-width: 100%;
		min-height: 44px;
		padding: 0.75rem 1.4rem;
		border-radius: 999px;
		background: var(--color-orange);
		color: var(--color-ink);
		font: 600 0.9rem var(--font-display);
		text-align: center;
		text-decoration: none;
		transition: background-color 0.15s ease;
	}

	a:hover { background: var(--color-orange-dark); }
	a:focus-visible { outline: 2px solid var(--color-text-inverse); outline-offset: 4px; }
	svg { flex-shrink: 0; transition: transform 0.2s ease; }
	a:hover svg { transform: translateX(3px); }

	@media (prefers-reduced-motion: reduce) {
		a, svg { transition: none; }
		a:hover svg { transform: none; }
	}

	@media (min-width: 640px) {
		.agenda-cta { flex-direction: row; align-items: center; justify-content: space-between; }
	}

	@media (min-width: 768px) {
		.agenda-cta { margin-block-start: 2.5rem; }
	}
</style>
